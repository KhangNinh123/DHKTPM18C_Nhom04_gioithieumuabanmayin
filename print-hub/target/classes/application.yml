server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/printshop?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:huynhtannha2003@gmail.com}
    password: ${MAIL_PASSWORD:cgah urjw psal rrvk}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 3000
          writetimeout: 5000
  ai:
    model:
      chat: google-genai
    google:
      genai:
        # ⚠️ CẢNH BÁO: API key trong file này chỉ dùng cho development
        # Trong production, nên sử dụng environment variable: ${GEMINI_API_KEY}
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-2.0-flash
            temperature: 0.5

file:
  upload-dir: ./uploads

jwt:
  secret: 123456789101234567891012345678910123456789101234567891012345678910
  expiration: 86400000 # 24 hours in milliseconds
  refresh-token:
    expiration: 604800000 # 7 days in milliseconds

app:
  email:
    from: ${MAIL_FROM:noreply@printshop.com}